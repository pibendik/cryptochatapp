// Placeholder widget test — replace with real widget tests as features are built.
//
// See: https://flutter.dev/docs/cookbook/testing/widget/introduction

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder — app smoke test', (WidgetTester tester) async {
    // TODO: add widget tests for individual screens once Riverpod providers
    // can be overridden with test doubles.
    expect(true, isTrue);
  });
}
